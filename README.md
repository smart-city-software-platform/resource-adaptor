# README

## Environment Setup

* Install RVM
* Run on terminal: ```$ rvm install 2.2.0```
* In the project directory, run: 
  * ```$ bundle install```
  * ```$ rake db:create```
  * ```$ rake db:migrate```
* Run the tests:
  * ```$ rspec```

## Configuration

